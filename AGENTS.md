# j4fun 项目说明

## 项目定位

本仓库包含两个相关但用途不同的中文网站：

- **j4fun**：仓库根目录下的中文小玩意合集。这里收录把创意快速做成可玩的 AI 辅助小项目，主要面向海外华人家庭和中文学习者。用户可能用英文提出开发需求，但网站文案和主要界面默认使用简体中文。
- **识本**：面向海外华人家庭的华文教材导览网站，部署内容位于 `shiben/`，开发项目位于 `shiben-dev/`。产品名称统一写作“识本”，功能描述可写作“华文教材库”或“华文教材导览”。它将 [TapXWorld/ChinaTextbook](https://github.com/TapXWorld/ChinaTextbook) 的公开目录以更方便浏览和阅读的网站形式呈现，帮助海外华人家庭继续了解国内教育。

两个产品有关联，但定位要保持清楚：j4fun 偏轻量、有趣、可直接玩的中文互动项目；识本偏正式、实用的教材资源导览。

## 目录结构

- `index.html`：j4fun 首页和小游戏入口。
- `poetry.html`：诗词好玩局，包含飞花令、诗句接龙和诗词浏览。
- `sudoku.html`：汉字数独。
- `strands.html`：字踪，参考 Strands 的藏字连线游戏。文件名是 `strands.html`，不是 `trends.html`。
- `assets/main.css`：j4fun 首页与小游戏共用的主题和页面样式。不要再把页面 CSS 写回 HTML 的 `<style>` 标签。
- `playground/`：历史版本或试验稿，不是当前生产页面；除非任务明确要求，否则不要同步修改。
- `shiben-dev/`：识本的 Node.js / React 开发项目，是识本功能开发的源代码。
- `shiben/`：识本的静态构建产物。不要把这里当作主要源代码手工维护；修改识本时优先在 `shiben-dev/` 开发、验证并重新构建到这里。

## j4fun 开发约定

- 保持现有中国传统配色和轻松、亲切的视觉语言；新增小游戏应能从根目录直接打开，并在 `index.html` 增加入口。
- 页面内容优先使用简体中文，代码标识符和开发注释可使用英文。
- 小游戏默认应无需注册、尽量纯前端运行，并清楚说明数据来源和隐私行为。
- 共享色彩、字体、页头、按钮、卡片和响应式规则应复用 `assets/main.css`；页面专属选择器应放在对应的页面作用域下，避免影响其他小游戏。
- 保持键盘焦点、移动端触控和 `prefers-reduced-motion` 支持。
- 修改后至少检查四个当前页面都能载入 `assets/main.css`，且 HTML 中没有重新出现内联 `<style>`。

## 识本开发约定

- 品牌名称统一为“识本”，首次或需要解释时写作“识本 · 华文教材库”。不要在同一层级混用多个产品名。
- 保持项目的公益导览定位，并明确它是 TapXWorld/ChinaTextbook 的非官方界面。
- 不复制或二次托管教材文件；目录和文件来自公开仓库，教材权利归相应权利人所有。
- 保留面向海外华人家庭的清楚、温暖、易用的中文体验。
- 在 `shiben-dev/` 修改后，使用该项目现有脚本进行类型检查和静态构建，再更新 `shiben/` 构建产物。

## 修改原则

- 先阅读当前实现，再做最小且完整的修改。
- 不覆盖无关的用户改动，不随意修改 `playground/` 历史稿。
- 保持静态页面无需构建即可运行；若修改识本，则遵循 `shiben-dev/package.json` 中已有的工具链。

## Git 与部署流程

- 完成修改后只做必要的本地检查，保留改动不提交，方便用户先在本地测试。
- 不要在每次修改后自动 commit、push 或部署。
- 只有用户明确说“deploy”时，才检查待提交改动、撰写合适的 commit message、提交到本地 Git，并运行 `deploy.sh` 发布到 Cloudflare Pages。
- 默认不 push 到 GitHub；只有用户明确要求时才执行 `git push`。
